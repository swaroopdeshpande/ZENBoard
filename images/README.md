# Images

These are placeholders. Replace them with real photos, **keeping the same
filenames** — the README references them by name, so nothing else needs editing.

| File | Where it appears | Suggested size |
|---|---|---|
| `hero.png` | Top of the main README | 1200 x 620 (wide) |
| `frame.png` | Gallery | 800 x 480 |
| `plugins.png` | Gallery | 800 x 480 |
| `led-strip.png` | Gallery | 800 x 480 |
| `presence.png` | Gallery | 800 x 480 |

Notes:

- `.jpg` works too, but then update the `<img src="...">` paths in `README.md`.
- Keep them reasonably small (under ~1 MB each); GitHub serves them on every
  page load of the repo.
- This repository is **public** — check photos for anything you would not want
  public in the background, and avoid screenshots of plugins that render
  personal data (calendar events, Amazon orders, family notes, the WiFi setup
  screen, which shows the access-point password).
